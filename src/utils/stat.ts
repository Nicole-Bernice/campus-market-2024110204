// src/utils/stat.ts
import { ITEM_TYPE } from "./constants"
// 统计四种帖子各有多少条
export function countByType(list: any[]) {
  return []
}
// 统计各个校区帖子数量
export function countByCampus(list: any[]) {
  return []
}
